apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  selector:
    matchLabels:
      app: nginx
  replicas: 3
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.19
        ports:
        - containerPort: 80
---
apiVersion: batch/v1
kind: Job
metadata:
  name: liquibase-runner-job-20240424172452222
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    job: liquibase-runner-job-20240424172452222
spec:
  parallelism: 1
  completions: 1
  activeDeadlineSeconds: 1800
  ttlSecondsAfterFinished: 2628000
  template:
    metadata:
      labels:
        job: liquibase-runner-job-20240424172452222
    spec:
      serviceAccountName: default
      containers:
        - name: liquibase-runner-job-20240424172452222
          image: busybox:glibc
          command: ["sh", "-c", "sleep 30 && echo success && exit 0"]
      restartPolicy: "Never"
  backoffLimit: 3