apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    matchLabels:
      app: nginx
  replicas: 3
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.19.1
        ports:
        - containerPort: 80
---
apiVersion: batch/v1
kind: Job
metadata:
  name: liquibase-runner-job-2024042417245
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    job: liquibase-runner-job-2024042417245
spec:
  parallelism: 1
  completions: 1
  activeDeadlineSeconds: 1800
  ttlSecondsAfterFinished: 2628000
  template:
    metadata:
      labels:
        job: liquibase-runner-job-2024042417245
    spec:
      serviceAccountName: default
      containers:
        - name: liquibase-runner-job-2024042417245
          image: busybox:glibc
          command: ["sh", "-c", "sleep 30 && echo success new && exit 0"]
      restartPolicy: "Never"
  backoffLimit: 3